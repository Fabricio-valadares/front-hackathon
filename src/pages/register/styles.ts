import { Box, Button } from "@chakra-ui/react";
import styled from "styled-components";

export const ContainerLinkRedirect = styled(Box)`
	cursor: pointer;
	margin-top: 10px;
	text-align: center;
	text-decoration: underline;
`;

export const ButtonSubmit = styled(Button)`
	color: white;
`;